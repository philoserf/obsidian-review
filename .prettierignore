# Ignored via the global core.excludesfile, so git hides it but prettier does not.
.issues/

# Generated bundle output.
main.js

# Generated by Showboat, and it quotes `cat -n` output verbatim. Prettier strips
# the trailing tab that `cat -n` emits for a blank line, so every regeneration
# would fail `--check` on whitespace the generator is right to produce.
WALKTHROUGH.md
